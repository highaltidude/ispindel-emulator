version: '3.8'
services:
  ispindel-emulator:
    build: .
    ports:
      - "3009"
    environment:
      - PORT=3009
      - TIME_MULTIPLIER=100
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
