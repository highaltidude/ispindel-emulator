version: '3.8'
services:
  ispindel-emulator:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - TIME_MULTIPLIER=100
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
